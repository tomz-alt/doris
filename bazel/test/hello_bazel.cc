// Apache Doris - Bazel Setup Validation Test
// This simple program tests that the Bazel workspace is configured correctly

#include <iostream>

int main() {
    std::cout << "Hello from Bazel!" << std::endl;
    std::cout << "Doris build system migration is working!" << std::endl;
    return 0;
}
